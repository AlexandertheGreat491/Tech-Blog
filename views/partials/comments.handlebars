<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta">
            {{user.username}}
        </div>
        <div class="text">
            {{body}}
        </div>
    </section>
    {{/each}}
</div>